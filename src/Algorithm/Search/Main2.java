package Algorithm.Search;

public class Main2 {
    public static void main(String[] args) {
        int  [] array  = { 0, 1, 4, 5, 10, 20, 45};
        int low = 0;
        int max = array.length - 1;
        int middle = (low + max)/2;
        int figure = 10;

        while (max >=low) {
            if (middle >= figure) {

            }
        }

    }
}
